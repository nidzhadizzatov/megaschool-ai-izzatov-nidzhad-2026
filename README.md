# GitHub SDLC Agent

Автоматическая система для разработки и ревью кода через GitHub Issues и Pull Requests.

## Что это

Два агента работают вместе:

**Part I - Issue Solver**
- Получает Issue через webhook
- Клонирует репо в `repos/{UUID}/`
- Спрашивает у GPT-4o-mini про каждый файл: "Есть ли тут эта проблема?"
- GPT отвечает: `{ issue_found: bool, code_correction: string }`
- Повторяет анализ-фикс до 3 раз на файл
- Создаёт PR
- Удаляет клон

**Part II - PR Reviewer**
- Получает PR через webhook
- Проверяет ТОЛЬКО изменённые файлы
- Для каждого файла: `{ issue_solved: bool, notes: string }`
- Пишет комментарий в PR

Всё работает через GitHub webhooks + worker'ы каждые 5 секунд.

## Фишки

- Каждый репо клонируется в `repos/{UUID}/` - нет конфликтов
- Файл `.github/agent_ignore.txt` - список файлов, которые агент не трогает
- Автоочистка после работы
- PR reviewer смотрит только изменённые файлы
- Supervisor перезапускает при падении

## Структура

```
.
├── .github/
│   ├── agent_config.yml          # Настройки агента
│   └── agent_ignore.txt          # Что не трогать
│
├── agent/                         # Основной код
│   ├── server.py                 # Webhook сервер
│   ├── worker.py                 # Issue solver (каждые 5 сек)
│   ├── pr_review_worker.py       # PR reviewer (каждые 5 сек)
│   ├── issue_solver.py           # Логика фикса
│   ├── pr_reviewer.py            # Логика ревью
│   ├── ai_client.py              # Работа с GPT
│   ├── repo_manager.py           # Git операции
│   ├── database.py               # TinyDB
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── supervisord.conf
│   └── requirements.txt
│
├── demo/                          # Тестовое приложение
│   ├── app.py
│   ├── broken_logic.py           # С багами специально
│   └── utils.py
│
├── .env.example
└── README.md
```

**Важно**: `.env` файл должен быть в `agent/.env`

## 🔄 Как это работает

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  User creates│─────▶│  GitHub      │─────▶│ Code Agent   │
│     Issue    │      │  Actions     │      │    (CLI)     │
└──────────────┘      │  triggered   │      └──────┬───────┘
                      └──────────────┘             │
                                                   ▼
                                          Analyze & Fix Code
                                                   │
                                                   ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  PR merged   │◀─────│  AI Reviewer │◀─────│  Create PR   │
│  Issue closed│      │    (CLI)     │      │              │
└──────────────┘      └──────────────┘      └──────────────┘
```

### Part I: Issue → PR Flow (через GitHub Actions)

```
Issue Created
      ↓
GitHub Actions Workflow
      ↓
python cli.py process-issue owner/repo 123
      ↓
Code Agent:
  - Clone to repos/{UUID}/
  - Analyze files (skip agent_ignore.txt)
  - Fix × 3 iterations
  - Create PR
  - Cleanup repos/{UUID}/
      ↓
PR Created
```

### Part II: PR → Review Flow (через GitHub Actions)

```
PR Created/Updated
      ↓
GitHub Actions Workflow
      ↓
python cli.py review-pr owner/repo 456
      ↓
AI Reviewer:
  - Get changed files only
  - Review each file
  - Post comment
      ↓
Review Complete
```
### Part I: Issue → PR Flow

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ Issue opened │─────▶│  Webhook     │─────▶│   TinyDB     │
│  on GitHub   │      │  server.py   │      │  (pending)   │
└──────────────┘      └──────────────┘      └──────────────┘
                                                    │
                                                    ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  PR created  │◀─────│ Issue Solver │◀─────│   Worker     │
│   + Issue    │      │ (fix loop    │      │  (every 5s)  │
│   closed     │      │  up to 3x)   │      └──────────────┘
└──────────────┘      └──────────────┘
       │
       │ (triggers pull_request webhook)
       ▼
┌──────────────┐
│ PR Review    │
│  (Part II)   │
└──────────────┘
```

### Part II: PR → Review Flow

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ PR opened/   │─────▶│  Webhook     │─────▶│   TinyDB     │
│ synchronized │      │  server.py   │      │ pr_reviews   │
└──────────────┘      └──────────────┘      │  (pending)   │
                                             └──────────────┘
                                                    │
                                                    ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  Comment to  │◀─────│ PR Reviewer  │◀─────│ PR Review    │
│  GitHub PR   │      │ (ONLY changed│      │  Worker      │
│ (all results)│      │   files)     │      │ (every 5s)   │
└──────────────┘      └──────────────┘      └──────────────┘
                            │
                            │ Format per file:
                            │ { issue_solved: bool, notes: string }
                            ▼
                      ✅ All passed → approve
                      ❌ Issues → comment with details
```

## Технологии

- Python 3.11+
- FastAPI (webhook сервер)
- OpenAI API (gpt-4o-mini)
- PyGithub, GitPython
- TinyDB (JSON база)
- Docker + Supervisor

## Архитектура

```
GitHub Webhook
      ↓
FastAPI Server (:8000)
      ↓
   TinyDB (db.json)
      ↓
  Workers (polling every 5s)
      ↓
┌─────────────────┬──────────────────┐
│  Issue Solver   │   PR Reviewer    │
│  - Clone repo   │   - Get changed  │
│    to repos/    │     files only   │
│    {UUID}/      │   - Review each  │
│  - Analyze all  │     file         │
│    (+ ignore)   │   - Post comment │
│  - Fix × 3      │                  │
│  - Create PR    │                  │
│  - Cleanup      │                  │
└─────────────────┴──────────────────┘
```

## 📋 Соответствие требованиям ТЗ

| Требование | Статус | Реализация |
|------------|--------|------------|
| Автоматическая обработка Issues/PR | ✅ | Webhook-based система (альтернатива GitHub Actions) |
| Code Agent (CLI) | ✅ | `agent/issue_solver.py` |
| AI Reviewer Agent (CLI) | ✅ | `agent/pr_reviewer.py` |
| Несколько итераций правок | ✅ | До 3 итераций на файл (настраивается) |
| Финальный PR | ✅ | Создаётся автоматически с описанием |
| Python 3.11+ | ✅ | Используется 3.11 |
| GPT-4o-mini | ✅ | Настраивается в `.env` |
| GitPython/PyGithub | ✅ | Используются обе библиотеки |
| Dockerfile + docker-compose | ✅ | `agent/Dockerfile` + `docker-compose.yml` |
| **Repos isolation** | ✅ | **`repos/{UUID}/` для каждого клона** |
| **Agent ignore** | ✅ | **`.github/agent_ignore.txt` поддержка** |
| **Auto cleanup** | ✅ | **Удаление после обработки** |

---

**Автор:** Иззатов Ниджад  
**Репозиторий:** https://github.com/nidzhadizzatov/megaschool-ai-izzatov-nidzhad-2026

