# Demo Application

Простое Python приложение с намеренными багами для тестирования Coding Agent.

## 📋 Описание

Это демонстрационное приложение содержит файлы с типичными багами, которые агент должен автоматически найти и исправить.

## 📁 Структура

```
demo/
├── app.py              # Главный файл приложения
├── broken_logic.py     # Файл с намеренными багами
├── utils.py            # Вспомогательные функции
├── tests/
│   └── test_broken_logic.py  # Тесты
└── README.md
```

## 🐛 Намеренные баги в broken_logic.py

### 1. Division by Zero
```python
def calculate_average(numbers):
    return sum(numbers) / len(numbers)  # ❌ Краш на пустом списке
```

**Должно быть:**
```python
def calculate_average(numbers):
    if not numbers:
        return 0
    return sum(numbers) / len(numbers)
```

### 2. Off-by-one Error
```python
def get_last_n_items(items, n):
    return items[-n:]  # ❌ Проблема когда n больше len(items)
```

## 🧪 Как тестировать

### Шаг 1: Создайте Issue

Перейдите в Issues → New Issue:

```markdown
Title: Fix division by zero in demo/broken_logic.py

Description:
The `calculate_average` function crashes when passed an empty list.

**Steps to reproduce:**
1. Call `calculate_average([])`
2. ZeroDivisionError is raised

**Expected behavior:**
Function should return 0 or None for empty lists.
```

### Шаг 2: Ожидайте

Coding Agent автоматически:
1. ✅ Получит webhook
2. ✅ Клонирует репо в `agent/repos/{UUID}/`
3. ✅ Найдёт и проанализирует `demo/broken_logic.py`
4. ✅ Определит проблему через ChatGPT
5. ✅ Исправит код (добавит проверку на пустой список)
6. ✅ Создаст PR
7. ✅ Добавит комментарий в Issue
8. ✅ Удалит локальную копию репозитория

### Шаг 3: Проверьте PR

1. Перейдите в Pull Requests
2. Найдите PR от Coding Agent
3. AI Reviewer автоматически добавит комментарий с анализом

## 🚀 Запуск тестов

```bash
# Установите зависимости
pip install pytest

# Запустите тесты (они должны ПАДАТЬ до фикса)
pytest demo/tests/ -v
```

---

**Happy Testing! 🚀**

3. PR Reviewer проверит исправление
